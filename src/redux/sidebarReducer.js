let initialState = {
    friends: [
        { id: 1, name: 'Max' },
        { id: 1, name: 'Andrew' },
        { id: 1, name: 'Vlad' },
        { id: 1, name: 'Valera' }
    ]
};


const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;